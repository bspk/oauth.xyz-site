import SEO from '../../components/seo'
import Sequence from '../../components/sequence'

<SEO title="Interaction" keywords={['oauth', 'authorization', 'security']} />


<Sequence code={`
participant dap
participant app
participant user
participant dac
participant dl
 
user-->dac: create data request
dac->dac: start transaction
dac->dl: register transaction details, key by interact URL
dac-->user: interact URL
user-->app: interact URL/code
user-->app: authenticate (local or federated)
app->dap: send interact URL
dap->dl: fetch transaction details, key by interact URL
user-->app: approve transaction
app->dap: assert references + ownership proof
dap->dl: assert references + ownership proof
dap->app: generate data decryption keys
 
`} />