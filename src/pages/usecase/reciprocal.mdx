# Description

A user needs to connect two services to each other, each side getting an access token to access the other's API.

# Why OAuth doesn't work

OAuth solves one directional sharing only, with the client calling the RS.

# What OAuth extensions are doing

The reciprocal OAuth extension hacks the token response to return an additional authorization code to continue the process. 

# How XYZ solves it

Since things start in the back channel, it's not 